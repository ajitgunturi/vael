---
phase: 13-planning-insights-integration-polish
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - lib/features/planning/screens/planning_dashboard_screen.dart
  - lib/features/planning/widgets/insight_alert_card.dart
  - lib/features/planning/screens/fi_calculator_screen.dart
  - lib/features/planning/screens/emergency_fund_screen.dart
  - lib/features/planning/screens/allocation_screen.dart
  - lib/features/planning/screens/milestone_dashboard_screen.dart
  - lib/features/planning/screens/savings_allocation_screen.dart
  - lib/features/planning/screens/opportunity_fund_screen.dart
  - lib/features/planning/screens/cash_flow_health_screen.dart
  - lib/features/planning/screens/lifetime_timeline_screen.dart
  - lib/features/cashflow/screens/cash_flow_screen.dart
  - lib/features/dashboard/screens/savings_rate_detail_screen.dart
  - test/features/planning/planning_dashboard_test.dart
autonomous: true
requirements: [NAV-01, NAV-08]

must_haves:
  truths:
    - "Planning dashboard shows alert cards above health grid when alerts exist"
    - "Alert cards use color-coded severity (critical=error, warning=amber, info=tertiary)"
    - "Every planning screen has empty-state with navigable CTA when prerequisites are missing"
    - "All planning screens have back navigation (no dead-end screens)"
  artifacts:
    - path: "lib/features/planning/widgets/insight_alert_card.dart"
      provides: "Alert card widget with severity coloring"
      contains: "class InsightAlertCard"
    - path: "lib/features/planning/screens/planning_dashboard_screen.dart"
      provides: "Dashboard with alerts section above health grid"
      contains: "InsightAlertCard"
  key_links:
    - from: "lib/features/planning/screens/planning_dashboard_screen.dart"
      to: "insightsProvider"
      via: "ref.watch"
      pattern: "insightsProvider"
    - from: "lib/features/planning/widgets/insight_alert_card.dart"
      to: "PlanningInsight"
      via: "renders insight data"
      pattern: "PlanningInsight"
---

<objective>
Wire alert display into planning dashboard and ensure all planning screens have proper empty states with navigable CTAs and no dead-end navigation.

Purpose: Makes alerts visible to users and ensures navigation coherence across all Phase 6-12 screens.
Output: Alert cards on dashboard, empty states on all planning screens, no dead-end screens.
</objective>

<execution_context>
@/Users/ajitg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-planning-insights-integration-polish/13-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From lib/core/financial/insights_engine.dart (created in Plan 01):
```dart
enum InsightSeverity { critical, warning, info }
enum InsightType { efBelowTarget, allocationOffTarget, fiDateSlipping, sinkingFundUnderfunded }
class PlanningInsight {
  final InsightType type;
  final InsightSeverity severity;
  final String title;
  final String description;
}
```

From lib/features/planning/providers/insights_provider.dart (created in Plan 01):
```dart
final insightsProvider = FutureProvider.family<List<PlanningInsight>, ({String familyId, String userId})>(...);
```

From lib/shared/widgets/empty_state.dart:
```dart
class EmptyState extends StatelessWidget {
  const EmptyState({required this.icon, required this.title, this.subtitle, this.actionLabel, this.onAction});
}
```

From lib/features/planning/widgets/health_metric_card.dart:
```dart
class HealthMetricCard extends StatelessWidget {
  // showSetup=true renders "Set up" CTA instead of value
  final bool showSetup;
  final String setupLabel;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: InsightAlertCard widget + dashboard alert section</name>
  <files>
    lib/features/planning/widgets/insight_alert_card.dart,
    lib/features/planning/screens/planning_dashboard_screen.dart,
    test/features/planning/planning_dashboard_test.dart
  </files>
  <read_first>
    lib/features/planning/screens/planning_dashboard_screen.dart,
    lib/features/planning/widgets/health_metric_card.dart,
    lib/shared/theme/color_tokens.dart,
    test/features/planning/planning_dashboard_test.dart,
    lib/core/financial/insights_engine.dart
  </read_first>
  <action>
    1. Create lib/features/planning/widgets/insight_alert_card.dart:
       - class InsightAlertCard extends StatelessWidget
       - Constructor: required PlanningInsight insight, VoidCallback? onTap
       - Build: Card with colored left border strip (4dp width):
         - critical: colorScheme.error
         - warning: Colors.amber.shade700
         - info: colorScheme.tertiary
       - Content: Row with severity icon (Icons.error for critical, Icons.warning_amber for warning, Icons.info_outline for info) + Column(title bold, description body)
       - Tappable via InkWell when onTap provided
       - Semantics label: "${insight.severity.name}: ${insight.title}"

    2. Update PlanningDashboardScreen to show alerts above health grid:
       - Watch insightsProvider((familyId: familyId, userId: userId))
       - If insights list is non-empty, render a Column of InsightAlertCard widgets above the _HealthGrid
       - Add a section header "Alerts" with Text style titleSmall + onSurfaceVariant color
       - Each alert card taps to navigate to the relevant screen:
         - efBelowTarget -> EmergencyFundScreen
         - allocationOffTarget -> AllocationScreen
         - fiDateSlipping -> FiCalculatorScreen
         - sinkingFundUnderfunded -> GoalListScreen (sinking funds tab)
       - When no alerts, show nothing (no empty alert section)

    3. Update planning_dashboard_test.dart:
       - Add test: "shows alert cards when insights provider returns alerts"
       - Override insightsProvider to return [PlanningInsight(type: efBelowTarget, severity: critical, ...)]
       - Verify InsightAlertCard is rendered with correct text
  </action>
  <verify>
    <automated>cd /Users/ajitg/workspace/vael && /Users/ajitg/fvm/versions/stable/bin/flutter test test/features/planning/planning_dashboard_test.dart -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class InsightAlertCard" lib/features/planning/widgets/insight_alert_card.dart
    - grep -q "insightsProvider" lib/features/planning/screens/planning_dashboard_screen.dart
    - grep -q "InsightAlertCard" lib/features/planning/screens/planning_dashboard_screen.dart
    - grep -q "Alerts" lib/features/planning/screens/planning_dashboard_screen.dart
  </acceptance_criteria>
  <done>Planning dashboard shows severity-colored alert cards above health grid. Alert cards navigate to relevant detail screens on tap.</done>
</task>

<task type="auto">
  <name>Task 2: Empty states with navigable CTAs on all planning screens + dead-end fixes</name>
  <files>
    lib/features/planning/screens/fi_calculator_screen.dart,
    lib/features/planning/screens/emergency_fund_screen.dart,
    lib/features/planning/screens/allocation_screen.dart,
    lib/features/planning/screens/milestone_dashboard_screen.dart,
    lib/features/planning/screens/savings_allocation_screen.dart,
    lib/features/planning/screens/opportunity_fund_screen.dart,
    lib/features/planning/screens/cash_flow_health_screen.dart,
    lib/features/planning/screens/lifetime_timeline_screen.dart,
    lib/features/cashflow/screens/cash_flow_screen.dart,
    lib/features/dashboard/screens/savings_rate_detail_screen.dart
  </files>
  <read_first>
    lib/features/planning/screens/fi_calculator_screen.dart,
    lib/features/planning/screens/emergency_fund_screen.dart,
    lib/features/planning/screens/allocation_screen.dart,
    lib/features/planning/screens/milestone_dashboard_screen.dart,
    lib/features/planning/screens/savings_allocation_screen.dart,
    lib/features/planning/screens/opportunity_fund_screen.dart,
    lib/features/planning/screens/cash_flow_health_screen.dart,
    lib/features/planning/screens/lifetime_timeline_screen.dart,
    lib/features/cashflow/screens/cash_flow_screen.dart,
    lib/features/dashboard/screens/savings_rate_detail_screen.dart,
    lib/shared/widgets/empty_state.dart
  </read_first>
  <action>
    Audit each planning screen for empty-state handling. For screens that already use EmptyState widget, verify the CTA navigates somewhere useful. For screens missing empty states, add them.

    Pattern to apply on each screen:
    - When the screen's primary data is null/empty AND prerequisites are missing, show EmptyState widget with:
      - Relevant icon
      - Clear title ("No life profile configured", "No emergency fund set up", etc.)
      - Subtitle explaining what the screen needs
      - actionLabel: "Set up Life Profile" / "Configure Emergency Fund" / etc.
      - onAction: Navigator.of(context).push to the setup screen

    Screens to audit and fix:
    1. FiCalculatorScreen -- already has standalone mode with placeholders, verify it works. No changes if standalone mode is functional.
    2. EmergencyFundScreen -- check if it handles no-data state. If monthlyEssentials is 0 and no accounts tagged, should show setup guidance.
    3. AllocationScreen -- if no life profile (no age for glide path lookup), show EmptyState with CTA to Life Profile.
    4. MilestoneDashboardScreen -- if no life profile, show EmptyState with CTA to Life Profile.
    5. SavingsAllocationScreen -- if no rules exist, should show EmptyState with "Create your first rule" CTA (likely already handled).
    6. OpportunityFundScreen -- if no accounts, show EmptyState. Likely already handled.
    7. CashFlowHealthScreen -- if no data, show EmptyState with CTA to add recurring rules.
    8. LifetimeTimelineScreen -- if no life profile, show EmptyState with CTA to Life Profile.
    9. CashFlowScreen -- if no recurring rules, show EmptyState.
    10. SavingsRateDetailScreen -- if no metrics data, show EmptyState.

    For each screen, import EmptyState from lib/shared/widgets/empty_state.dart if not already imported.

    Also verify all screens have AppBar with back button (default behavior with Navigator.push). Scaffold with AppBar automatically gets back button -- just verify no screen overrides automaticallyImplyLeading to false without providing alternative navigation.

    NAV-01 verification: All screens are reachable from HomeShell because:
    - Dashboard -> Financial Health card -> PlanningDashboardScreen -> each metric card -> detail screens
    - Dashboard -> Cash Flow quick action -> CashFlowScreen
    - Dashboard -> Life Plan quick action -> LifetimeTimelineScreen
    - Settings -> Financial Planning tiles -> all setup screens
    - Goals tab -> goal detail -> related screens
    Verify no screen is only reachable via a route that has been removed.
  </action>
  <verify>
    <automated>cd /Users/ajitg/workspace/vael && /Users/ajitg/fvm/versions/stable/bin/flutter test test/features/planning/ -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -rq "EmptyState" lib/features/planning/screens/allocation_screen.dart
    - grep -rq "EmptyState" lib/features/planning/screens/milestone_dashboard_screen.dart
    - grep -rq "EmptyState" lib/features/planning/screens/lifetime_timeline_screen.dart
    - grep -rq "EmptyState" lib/features/planning/screens/cash_flow_health_screen.dart
  </acceptance_criteria>
  <done>Every planning screen shows EmptyState with navigable CTA when prerequisites are missing. No dead-end screens exist. All screens reachable from HomeShell via tap chain.</done>
</task>

</tasks>

<verification>
- Planning dashboard renders alert section above health grid
- All planning screens have empty-state fallback with navigable CTA
- No dead-end screens (all have AppBar back button)
- All screens reachable from HomeShell
</verification>

<success_criteria>
- InsightAlertCard widget renders severity-colored alert cards
- Planning dashboard shows alerts from insightsProvider
- All 10+ planning screens have empty states with setup CTAs
- NAV-01 satisfied: all screens reachable from HomeShell
</success_criteria>

<output>
After completion, create `.planning/phases/13-planning-insights-integration-polish/13-02-SUMMARY.md`
</output>
