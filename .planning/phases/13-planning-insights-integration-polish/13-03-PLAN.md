---
phase: 13-planning-insights-integration-polish
plan: 03
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - test/integration/planning_navigation_test.dart
autonomous: true
requirements: [NAV-09, NAV-10]

must_haves:
  truths:
    - "Navigation integration tests pass at 400dp, 750dp, and 1200dp breakpoints"
    - "Tests verify round-trip navigation (push then pop) preserves back stack"
    - "Minimum 32 integration tests covering all planning screen navigation paths"
    - "Cross-feature navigation verified: dashboard -> detail -> back to dashboard"
  artifacts:
    - path: "test/integration/planning_navigation_test.dart"
      provides: "Navigation integration test suite"
      min_lines: 400
      contains: "planning_navigation"
  key_links:
    - from: "test/integration/planning_navigation_test.dart"
      to: "HomeShell"
      via: "pumps HomeShell and navigates"
      pattern: "HomeShell|ProviderScope"
    - from: "test/integration/planning_navigation_test.dart"
      to: "PlanningDashboardScreen"
      via: "taps Financial Health card"
      pattern: "Financial Health|PlanningDashboardScreen"
---

<objective>
Create comprehensive navigation integration tests verifying all planning screens are reachable and navigable at 3 responsive breakpoints with cross-feature round-trip verification.

Purpose: Ensures navigation coherence and responsive layout correctness across the entire planning feature set.
Output: 32+ integration tests at 400dp, 750dp, 1200dp breakpoints.
</objective>

<execution_context>
@/Users/ajitg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-planning-insights-integration-polish/13-01-SUMMARY.md

<interfaces>
<!-- Navigation targets to test -->

Screens reachable from Dashboard:
- PlanningDashboardScreen (via Financial Health summary card "View All")
- CashFlowScreen (via Cash Flow quick action)
- LifetimeTimelineScreen (via Life Plan quick action)

Screens reachable from PlanningDashboardScreen:
- FiCalculatorScreen (via FI Progress card tap)
- EmergencyFundScreen (via EF Coverage card tap)
- SavingsRateDetailScreen (via Savings Rate card tap)
- MilestoneDashboardScreen (via Milestones card tap)
- LifeProfileWizardScreen (via "Set up" CTA on unconfigured cards)

Screens reachable from Settings > Financial Planning:
- LifeProfileWizardScreen
- AllocationScreen
- EmergencyFundScreen
- SavingsAllocationScreen
- OpportunityFundScreen
- CashFlowScreen
- FiCalculatorScreen
- MilestoneDashboardScreen
- DecisionModelerScreen

From lib/shared/shell/home_shell.dart:
```dart
class HomeShell extends ConsumerStatefulWidget {
  const HomeShell({super.key, required this.familyId, required this.userId});
}
```

From lib/shared/layout/adaptive_scaffold.dart:
```dart
// Responsive breakpoints: phone (<750), tablet (750-1200), desktop (1200+)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Navigation integration tests at 3 breakpoints (32+ tests)</name>
  <files>
    test/integration/planning_navigation_test.dart
  </files>
  <read_first>
    lib/shared/shell/home_shell.dart,
    lib/shared/layout/adaptive_scaffold.dart,
    lib/features/dashboard/screens/dashboard_screen.dart,
    lib/features/planning/screens/planning_dashboard_screen.dart,
    lib/features/settings/screens/settings_screen.dart,
    test/integration/phase5_e2e_test.dart,
    lib/features/planning/providers/planning_health_providers.dart,
    lib/features/planning/providers/insights_provider.dart
  </read_first>
  <action>
    Create test/integration/planning_navigation_test.dart with comprehensive navigation tests.

    Test structure:
    ```
    void main() {
      group('Planning Navigation - Phone (400dp)', () { ... });
      group('Planning Navigation - Tablet (750dp)', () { ... });
      group('Planning Navigation - Desktop (1200dp)', () { ... });
      group('Cross-feature round-trips', () { ... });
    }
    ```

    Helper setup:
    - Create a _buildTestApp helper that wraps HomeShell in ProviderScope with all necessary provider overrides:
      - Override planningHealthProvider to return mock PlanningHealthData (avoid drift DB)
      - Override insightsProvider to return empty list
      - Override sessionFamilyIdProvider and sessionUserIdProvider
      - Override any StreamProvider that would need a real DB (dashboardDataProvider, etc.)
    - Create _setBreakpoint(WidgetTester tester, double width) helper that sets tester.view.physicalSize and devicePixelRatio

    Phone (400dp) tests (11 tests):
    1. Dashboard shows Financial Health summary card
    2. Tap "View All" on Financial Health -> navigates to PlanningDashboardScreen
    3. Tap "Cash Flow" quick action -> navigates to CashFlowScreen
    4. Tap "Life Plan" quick action -> navigates to LifetimeTimelineScreen
    5. Settings tab -> Financial Planning section visible with all tiles
    6. Settings -> Life Profile tile -> navigates to LifeProfileWizardScreen
    7. Settings -> Emergency Fund tile -> navigates to EmergencyFundScreen
    8. Settings -> Allocation Targets tile -> navigates to AllocationScreen
    9. Settings -> Savings Allocation tile -> navigates to SavingsAllocationScreen
    10. Settings -> Opportunity Fund tile -> navigates to OpportunityFundScreen
    11. Settings -> Cash Flow tile -> navigates to CashFlowScreen

    Tablet (750dp) tests (11 tests):
    - Same 11 navigation paths, verified at 750dp width
    - Verify layout adapts (if AdaptiveScaffold uses side rail at this width)

    Desktop (1200dp) tests (5 tests):
    - Key navigation paths verified at 1200dp
    - Verify 3-column layout if applicable

    Cross-feature round-trip tests (5+ tests):
    12. Dashboard -> PlanningDashboard -> FI Calculator -> back -> back -> Dashboard (verify back stack)
    13. Dashboard -> PlanningDashboard -> EF Coverage -> back -> verify PlanningDashboard still visible
    14. Settings -> Life Profile -> back -> verify Settings still visible
    15. Dashboard -> CashFlow -> back -> verify Dashboard visible
    16. Settings -> Emergency Fund -> back -> Settings -> Allocation -> back -> Settings (multi-hop)

    Each test:
    - Use pumpWidget with _buildTestApp
    - Set breakpoint size
    - Find and tap navigation target
    - pumpAndSettle
    - Verify destination screen is visible (find by type or by key text)
    - For round-trip: tap back button, pumpAndSettle, verify previous screen

    Important patterns (from project history):
    - Override planningHealthProvider in ALL tests rendering DashboardScreen
    - Use tester.view.physicalSize for breakpoint setting
    - Use addTearDown to reset physicalSize
    - Use scrollUntilVisible for Settings tiles that may be off-screen
    - Provider override pattern consistent with Phase 7-12 widget tests
  </action>
  <verify>
    <automated>cd /Users/ajitg/workspace/vael && /Users/ajitg/fvm/versions/stable/bin/flutter test test/integration/planning_navigation_test.dart -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "testWidgets" test/integration/planning_navigation_test.dart returns >= 32
    - grep -q "400" test/integration/planning_navigation_test.dart
    - grep -q "750" test/integration/planning_navigation_test.dart
    - grep -q "1200" test/integration/planning_navigation_test.dart
    - grep -q "round-trip" test/integration/planning_navigation_test.dart
    - grep -q "HomeShell" test/integration/planning_navigation_test.dart
  </acceptance_criteria>
  <done>32+ navigation integration tests pass at 3 breakpoints. Cross-feature round-trip tests verify back stack coherence. All planning screens reachable via tap chain from HomeShell.</done>
</task>

</tasks>

<verification>
- test/integration/planning_navigation_test.dart passes with 32+ tests
- Tests cover phone (400dp), tablet (750dp), desktop (1200dp) breakpoints
- Round-trip tests verify push/pop navigation coherence
- All planning screens verified reachable from HomeShell
</verification>

<success_criteria>
- 32+ testWidgets in planning_navigation_test.dart
- All tests pass at all 3 breakpoints
- Cross-feature round-trip navigation verified (NAV-10)
- No dead-end screens detected in navigation paths
</success_criteria>

<output>
After completion, create `.planning/phases/13-planning-insights-integration-polish/13-03-SUMMARY.md`
</output>
